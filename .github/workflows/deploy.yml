name: Build and Deploy to Hostinger via FTP
run-name: ${{ github.actor}} build and deploy the application

on:
  push:
    branches:
      - staging

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: List files
        run: ls -la

      - name: Change working directory
        run: cd meet-the-music-front-end

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "14"
          cache: "npm"
          cache-dependency-path: "meet-the-music-front-end/package-lock.json"

      - name: Install dependencies
        run: npm install

      - name: Build
        run: ng build --configuration production

      - name: FTP Deploy
        uses: SamKirkland/FTP-Deploy-Action@4.0.0
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: dist/meet-the-music-front-end/browser
          server-dir: ${{ secrets.FTP_SERVER_DIR}}
